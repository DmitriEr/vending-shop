export interface TypeItems {
  number: number;
  name: String;
  picture: String;
  price: number;
  count: number;
}

export interface TypeColumns {
  title: string,
  dataIndex: string,
  key: string,
}